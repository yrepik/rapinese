## Requirements
* PHP 5.6
* Composer
* Node JS
* Grunt CLI

## Installation
Copy .env_example file as .env and edit the values for the configuration variables.

    $ composer install
    $ npm install
    $ grunt install
